file(GLOB_RECURSE TEST_SOURCES *.cpp)

LINK_DIRECTORIES(${CMAKE_SOURCE_DIR}/lib) 
ADD_EXECUTABLE(nsp-test ${TEST_SOURCES})
TARGET_LINK_LIBRARIES(nsp-test ${EXTRA_LIBS})
TARGET_LINK_LIBRARIES(nsp-test jian)

INSTALL(TARGETS nsp-test DESTINATION bin)


