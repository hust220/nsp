#ifndef SCORE_H
#define SCORE_H

#include "DistAnal.h"
#include "DihAnal.h"

namespace jian {

class Score {
public:
	Score();
	Score(char *);
	double run(Obj<RNA>);

	Obj<DistAnal> distAnal;
	Obj<DihAnal> dihAnal;

private:
	string par_dist_obs;
	string par_dist_ref;
	string dihPar;
	string reference_state;
	int cutoff;
	double bin;
	double distWeight;
	double dihWeight;
};

} /// namespace jian

#endif

