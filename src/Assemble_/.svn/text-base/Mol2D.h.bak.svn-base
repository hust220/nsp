#ifndef MOL2D_H
#define MOL2D_H

#include "loop.h"

class Mol2D {
public:
	Mol2D(string, int = 0);
	Mol2D(string, string, int = 0);
	~Mol2D();

	void setTree(vector<res> &, int = 0);
	void resetNum(loop *, int *);
	void print();
	void readSeq(string);
	void readMol(string);
	loop *head;

private:
	void getLoop(vector<res> &, vector<loop *> &, int *);
	void printTree(loop *);
	void delLoop(loop *);
	void setSeq(loop *, string);

	RNA *mol;
	string line;
	string seq;
	int view;
};

#endif
